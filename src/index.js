/* eslint no-unused-vars: off */

/**
 *  Import only sass files that is needed to generate a css file.
 *  Use _ prefixed sass on css utils
 *
 *  See: Imports at https://sass-lang.com/guide
 */
import style from './styles/main.sass';

/**
 *  Import all js file here instead at pug files.
 */
import plugin from './scripts/plugin';

console.log('lock n loaded!');
